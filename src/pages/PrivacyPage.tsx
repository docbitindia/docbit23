import React from 'react';
import { Header } from '../components/Header';
import { Footer } from '../components/Footer';
import { usePageMeta } from '../hooks/usePageMeta';
export function PrivacyPage(){usePageMeta({path:'/privacy',title:'Privacy — DocBit',description:'How DocBit handles authentication, project data and uploaded files.'});return <div className="min-h-screen bg-white"><Header/><main className="mx-auto max-w-4xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Privacy</p><h1 className="public-title">Your data should have a clear boundary.</h1><p className="public-lead">DocBit is designed to distinguish local processing from cloud persistence. When you save a file to a project, it becomes part of your cloud workspace.</p><div className="prose-doc"><h2>Local processing</h2><p>Common Excel, CSV and JSON parsing and transformation can happen in your browser, which avoids unnecessary server transfers.</p><h2>Cloud projects</h2><p>Files and project metadata that you explicitly save to a cloud project are stored using the configured Supabase services and protected by authentication and Postgres Row Level Security and Storage policies.</p><h2>Authentication</h2><p>Account authentication is handled through Supabase Auth. Server-side administrative credentials are never intended to be exposed to the browser.</p><h2>Payments</h2><p>Paid billing is designed to use Razorpay through trusted server-side functions. Payment secrets and webhook verification keys are not client-side data.</p><p className="text-xs text-slate-400">Replace this product-level privacy summary with your final legal privacy notice before launch.</p></div></main><Footer/></div>}
