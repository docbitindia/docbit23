import React from 'react';import {Settings,Bell,ShieldCheck,UserCircle,ArrowRight} from 'lucide-react';import {AppShell} from '../components/AppShell';import {navigate} from '../router/useRoute';
export function SettingsPage(){return <AppShell><div className="mx-auto max-w-4xl px-5 py-8 sm:px-8"><p className="text-sm font-medium text-blue-600">Workspace preferences</p><h1 className="mt-1 text-3xl font-semibold tracking-[-.04em]">Settings</h1><p className="mt-2 text-sm text-slate-500">Manage workspace preferences, notifications and account controls.</p><div className="mt-8 grid gap-4"><SettingCard icon={Settings} title="Workspace" copy="Configure how your data workspace behaves as you work with files and projects."/><SettingCard icon={Bell} title="Notifications" copy="Notification controls are ready for future project, usage and billing alerts."/><SettingCard icon={ShieldCheck} title="Security" copy="Manage passwords, sessions, deactivation and account removal from Profile & Security." action="Open security" onClick={()=>navigate('/profile')}/><SettingCard icon={UserCircle} title="Profile" copy="Update your full name, profile photo and account identity." action="Open profile" onClick={()=>navigate('/profile')}/></div></div></AppShell>}
function SettingCard({icon:Icon,title,copy,action,onClick}:{icon:React.ElementType;title:string;copy:string;action?:string;onClick?:()=>void}){return <div className="surface-card flex items-start gap-4 p-6"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-slate-100 text-slate-600"><Icon size={18}/></span><div className="min-w-0 flex-1"><h2 className="font-semibold">{title}</h2><p className="mt-1 text-sm leading-6 text-slate-500">{copy}</p>{action&&<button className="mt-4 inline-flex items-center gap-1.5 text-sm font-semibold text-blue-600" onClick={onClick}>{action}<ArrowRight size={14}/></button>}</div></div>}
